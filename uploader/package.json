{
  "name": "sts-uploader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright uploader: people.json -> STS backend personnel form (reads loader output only, never writes out/)",
  "scripts": {
    "map-check": "node map-check.mjs",
    "detect": "node detect.mjs",
    "upload": "node upload-people.mjs"
  },
  "dependencies": {
    "playwright-core": "^1.49.1"
  },
  "engines": {
    "node": ">=18"
  }
}

{
  "_note": "OPTIONAL intentional merges: '<url substring>' -> '<backend target group name>'. URLs with no entry upload under their own stable source identity (slug of the source URL). Matching is substring, first hit wins. Normal zero-map workflow needs no entry here: adding a website = adding its input URL, never editing this file.",
  "map": {}
}
